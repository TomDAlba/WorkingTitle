<div id="top" class="wrap clearfix four columns ">
  <div id="main-content" class="container clearfix">
    <div class="row">
	    <div class="twelve columns">
	      <h2>
	        WriteMeDaily is a web application focused on encouraging one's creative side through writing. We provide a place for people to create and share their works with other members of the community, personalized writing prompts to encourage you when you are feeling uninspired, and an achievement system to encourage better writing habits!
	      </h2>
          <% @users.each_with_index do |user, index| %>
            <div class="panel1" onMouseOver="this.style.background='<%=user.get_color%>'" onMouseOut="this.style.background='#000000'">
            
            <%  link = "" %>
            <% if user.external_link.present? %>
              <% link = user.external_link %>
            <% else %>
              <% link =  root_url(:subdomain => user.subdomain, :port => 80) %>
            <% end %>
            <%= link_to link, :target => "_blank" do %>
            
              <h3>
                <span class="noshow">
                  <%= user.full_name %>
                </span>
                <span class="show">
                  <%= user.skill.upcase %>
                </span>
              </h3>
            <% end %>
            </div>		           			
          <% end %>
      </div>
    </div>
    
    <%= render "layouts/shared/footer"%>
